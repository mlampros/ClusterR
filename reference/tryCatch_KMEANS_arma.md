# tryCatch function to prevent armadillo errors in KMEANS_arma

tryCatch function to prevent armadillo errors in KMEANS_arma

## Usage

``` r
tryCatch_KMEANS_arma(
  data,
  clusters,
  n_iter,
  verbose,
  seed_mode,
  CENTROIDS,
  seed
)
```
