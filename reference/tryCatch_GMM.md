# tryCatch function to prevent armadillo errors

tryCatch function to prevent armadillo errors

## Usage

``` r
tryCatch_GMM(
  data,
  gaussian_comps,
  dist_mode,
  seed_mode,
  km_iter,
  em_iter,
  verbose,
  var_floor,
  seed,
  full_covariance_matrices
)
```
